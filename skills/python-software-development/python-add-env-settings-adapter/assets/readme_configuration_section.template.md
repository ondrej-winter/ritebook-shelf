## Configuration

Runtime configuration is loaded from environment variables and optional `.env`
files at startup.

See `docs/configuration.md` for the complete settings reference, including every
supported environment variable, default, example value, and secret-handling note.
Use `.env.example` as a safe local-development starting point.
